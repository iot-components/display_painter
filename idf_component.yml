version: "0.0.1" # Component version, required only for components pushed to the service
targets: # List of supported targets (optional, if missing all targets are considered to be supported)
  - esp32
  - esp32s2
  - esp32s3
description: ESP-IoT-Solution Display Painter # Description (optional)
url: https://github.com/iot-components/display_painter # Original repository (optional)
dependencies:
  # Required IDF version
  idf:
    version: ">=4.3"
  display_screen:
    git: https://github.com/iot-components/display_screen.git
    public: true

